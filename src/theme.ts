const colors = {
	primary: "#4054CC",
	warning: "#FF8C00",
	delete: "#FB3717",
	white: "#FFF",
	disabled: "#778899",
	dark: "#000",
};

const fonts = {
	default: "Avenir Medium",
};

const fontWeights = {
	semiBold: 500,
	bold: 600,
};

const theme = { colors, fonts, fontWeights };

export default theme;
